{
  "description": "Defines the Firestore structure for the Lexical Editor module settings.",
  "presets_document": {
    "document_path_template": "Global/master/config/modules/lexical-editor/presets",
    "description": "A single document that holds multiple presets as a map.",
    "fields": {
      "presets": {
        "type": "map",
        "description": "A map where each key is a preset ID (e.g., 'invite_email', 'default') and the value is the preset object.",
        "example_preset_object": {
          "name": { "type": "string", "description": "User-friendly name for the preset." },
          "content": { "type": "string", "description": "The Lexical editor state as a JSON string." },
          "settings": { "type": "map", "description": "Toolbar configuration for this preset.", "optional": true }
        }
      }
    }
  },
  "toolbar_config_document": {
    "document_path_template": "Global/master/config/modules/lexical-editor/default_toolbar_config",
    "description": "Stores the default toolbar configuration for the Lexical editor.",
    "fields": {
      "toolbar": {
        "type": "array",
        "description": "An ordered array of objects representing the toolbar items.",
        "items": {
          "type": "map",
          "fields": {
            "key": "string",
            "label": "string",
            "icon": "string",
            "active": "boolean"
          }
        }
      }
    }
  }
}

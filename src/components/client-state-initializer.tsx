// src/components/client-state-initializer.tsx
"use client";

import { useEffect } from 'react';
// A lógica de tema foi removida daqui, pois agora é gerenciada pelo ThemeProvider via cookies.

// Este componente não renderiza nada na UI e, por enquanto, não tem mais funções.
// Pode ser removido em uma futura limpeza do código.
export function ClientStateInitializer() {
  return null;
}

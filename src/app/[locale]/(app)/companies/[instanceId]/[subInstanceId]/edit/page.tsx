// PROXY PAGE (Nested)
// Corrected import path to point to the master edit page within the 'companies' feature folder.
import EditCompanyMasterPage from "../../../edit/page";

export default EditCompanyMasterPage;

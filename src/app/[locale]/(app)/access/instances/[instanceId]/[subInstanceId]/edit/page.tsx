// PROXY PAGE
// This also points to the master edit page. The master page is smart enough
// to detect the `subInstanceId` from the URL params and load the sub-instance data instead.
import EditInstanceMasterPage from "../../../edit/page";

export default EditInstanceMasterPage;

// src/app/[locale]/(app)/access/instances/[instanceId]/create/page.tsx

// PROXY PAGE for creating a sub-instance.
// It re-exports the master component from the base create page.
import CreateInstanceMasterPage from "../../create/page";

export default CreateInstanceMasterPage;

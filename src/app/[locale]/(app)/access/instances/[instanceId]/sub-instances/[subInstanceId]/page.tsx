// PROXY PAGE
// This page points to the master instance details page. The master page is smart enough
// to detect the `subInstanceId` from the URL params and load the sub-instance data instead.
import InstanceDetailsMasterPage from '../../page'; // CORREÇÃO: Caminho corrigido para o Master

export default InstanceDetailsMasterPage;

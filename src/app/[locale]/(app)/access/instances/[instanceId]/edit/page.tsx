// src/app/[locale]/(app)/access/instances/[instanceId]/edit/page.tsx

// PROXY PAGE
// This points to the master edit page, which will use the `instanceId` param
// to fetch and display the correct instance data.
import EditInstanceMasterPage from "../../edit/page";

export default EditInstanceMasterPage;

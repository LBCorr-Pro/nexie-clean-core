// This is the PROXY component.
// It re-exports the master component from the parent directory,
// passing along the route params.
import UsersMasterPage from "../page";

export default UsersMasterPage;
